;--------------------------------------------------------------------------
;   Variables
;--------------------------------------------------------------------------

(setq tab-width 4)

;--------------------------------------------------------------------------
;   C-Mode
;--------------------------------------------------------------------------

(add-hook 'c-mode-hook
    (function (lambda ()
    	  (setq tab-width 4))))

;--------------------------------------------------------------------------
;   C++-Mode
;--------------------------------------------------------------------------

(add-hook 'c++-mode-hook
    (function (lambda ()
    	  (setq tab-width 4))))
